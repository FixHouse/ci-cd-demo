name: Run Bash Script

on: [push]

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run simple Bash command
      run: |
        echo "Привіт від GitHub Actions 👋"
        echo "Запускаю CI/CD вперше!"
